---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: cronprojects.cronops.io
spec:
  group: cronops.io
  names:
    kind: CronProject
    listKind: CronProjectList
    plural: cronprojects
    shortNames:
    - cproj
    singular: cronproject
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.description
      name: Description
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CronProject groups HttpCronJobs, similar to ArgoCD AppProjects.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              CronProjectSpec describes a project grouping HttpCronJobs.
              Jobs join a project via their spec.project field; v0.2 uses projects for
              grouping and filtering only, per-project restrictions arrive in v0.3.
            properties:
              description:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
